var arr =["assignment", "problem", "media", "upload"];
var cry = arr.filter(function(element){
    if(element[0]==="a"|| element[element.length-1==="a"])
    {
        return (element);
    }
});
console.log(cry);